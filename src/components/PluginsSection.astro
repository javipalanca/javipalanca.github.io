---
// Plugins Section component
export interface Props {
    plugins: Array<{
        name: string;
        description: string;
        icon: string;
        githubUrl: string;
        docsUrl: string;
        pluginUrl: string;
        featured?: boolean;
    }>;
}

const { plugins } = Astro.props;
import PluginCard from './PluginCard.astro';
---

<section id="plugins" class="plugins-section py-5">
    <div class="container">
        <div class="text-center mb-5 fade-in-up">
            <h2 class="fw-bold">Extend SPADE with Plugins</h2>
            <p class="lead text-muted">Enhance your agent platform with specialized capabilities</p>
        </div>
        <div class="row g-4">
            {plugins.map((plugin, index) => (
                <div class={`col-md-6 col-lg-4 scale-in stagger-${index + 1}`}>
                    <PluginCard plugin={plugin} />
                </div>
            ))}
            <div class="col-md-6 col-lg-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">Explore All Plugins</h5>
                        <p class="card-text">Discover the complete SPADE plugin ecosystem with detailed documentation and examples.</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="/plugins" class="btn btn-sm btn-primary">
                                <i class="bi bi-collection me-1"></i>Browse All
                            </a>
                            <a href="https://github.com/javipalanca/spade/discussions" class="btn btn-sm btn-outline-primary">Suggest Ideas</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .plugins-section {
        background-color: var(--light-color);
    }

    /* Dark mode sections */
    :global(body.dark-mode) .plugins-section {
        background-color: var(--dark-bg) !important;
    }

    /* Plugin showcase */
    .plugin-card {
        background: white;
        border: 1px solid #e9ecef;
        border-radius: 10px;
        padding: 20px;
        transition: all 0.3s ease;
        height: 100%;
    }

    .plugin-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }

    .plugin-icon {
        width: 60px;
        height: 60px;
        background: linear-gradient(
            45deg,
            var(--primary-color),
            var(--secondary-color)
        );
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.5rem;
        margin-bottom: 15px;
    }
</style>
